name: dart_xml_parser
description: A sample command-line application.

version: 1.0.0

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  xml_map_converter: ^1.0.0+1

dev_dependencies:
  lints: ^1.0.0
  test: ^1.16.0
